# Copy to .env.dev / .env.staging / .env.prod (gitignored) and fill in
# real values. Consumed at compile time via `envied` (see lib/core/config/).

API_BASE_URL=https://api.example.com
ENABLE_VERBOSE_LOGGING=true
